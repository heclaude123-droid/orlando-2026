# =========================================================
# 隐私白名单：只发布 docs/ 公开站点。
# 其余全部本地文件（trip_state.yaml / 各 .md / 旧 HTML，含确认号·全名·金额）
# 一律不进 repo —— 即使 repo 设为 public 也不会泄露 PII。
# =========================================================
/*
!/docs/
!/.gitignore
